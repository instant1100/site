import React from 'react';

import InDeveloping from '@/core/features/InDeveloping/InDeveloping';

const Subscribe = (props) => {
  return <InDeveloping {...props} />
};

export default Subscribe;
