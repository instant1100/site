import React, { FC } from 'react';

import InDeveloping from '../InDeveloping/InDeveloping';
import { InDevelopingProps } from '../interfaces';

const Subscribe: FC<InDevelopingProps> = (props) => <InDeveloping {...props} />;

export default Subscribe;
